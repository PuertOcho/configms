server:
  port: ${PORT_CONFIG}

spring:
  application:
    name: ${CONFIG_SERVICE}
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations